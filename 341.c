#include <stdio.h>

int loop(int i, int j)
{
	if (/* condition */)
	{
		/* code */
	}
}
int main(int argc, char const *argv[])
{
	int j;//j==numero de jogos;
	scanf("%d", &j);
	loop(0, j);
	return 0;
}